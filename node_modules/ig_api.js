//api.js
var http = require("http");
var url = require('url');
var express = require('express');

exports.loggedIn = function(req, res){
  var parsedUrl = url.parse(req, true);
  var hash = parsedUrl.hash;
  res.send(hash);
};
